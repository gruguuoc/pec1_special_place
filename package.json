{
  "name": "pec1_special_place",
  "source": "./src/index.html",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, ie <= 11",
  "scripts": {
    "start": "npm-run-all clean parcel:dev",
    "build": "npm-run-all clean parcel:build optimicehtml",
    "parcel:dev": "parcel",
    "parcel:build": "parcel build",
    "clean": "rimraf dist .parcel-cache",
    "optimicehtml": "html-minifier-terser --input-dir dist --output-dir dist --remove-attribute-quotes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.12.0",
    "posthtml-include": "^2.0.1",
    "rimraf": "^6.0.1"
  },
  "targets": {
    "default": {
      "publicUrl": "./"
    }
  },
  "dependencies": {
    "jquery": "^3.7.1",
    "slick-carousel": "^1.8.1"
  }
}
